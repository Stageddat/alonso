/* eslint-disable no-unused-vars */
export enum userStatus {
	userFound = 'User found in the database',
	userNotFound = 'User not found in the database',
	userRegistered = 'User registered in the database',
	newUserRegistered = 'New user registered in the database',
}
